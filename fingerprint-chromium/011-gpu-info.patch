diff --git a/third_party/blink/renderer/modules/webgl/BUILD.gn b/third_party/blink/renderer/modules/webgl/BUILD.gn
index 7873c1698d..8ff4e4f70d 100644
--- a/third_party/blink/renderer/modules/webgl/BUILD.gn
+++ b/third_party/blink/renderer/modules/webgl/BUILD.gn
@@ -48,6 +48,8 @@ blink_modules_sources("webgl") {
     "ext_texture_norm_16.cc",
     "ext_texture_norm_16.h",
     "gl_string_query.h",
+    "gpu_fingerprint.cc",
+    "gpu_fingerprint.h",
     "khr_parallel_shader_compile.cc",
     "khr_parallel_shader_compile.h",
     "nv_shader_noperspective_interpolation.cc",
diff --git a/third_party/blink/renderer/modules/webgl/gpu_fingerprint.cc b/third_party/blink/renderer/modules/webgl/gpu_fingerprint.cc
new file mode 100644
index 0000000000..ee620485a9
--- /dev/null
+++ b/third_party/blink/renderer/modules/webgl/gpu_fingerprint.cc
@@ -0,0 +1,26 @@
+// Copyright (c) 2020 The ungoogled-chromium Authors. All rights reserved.
+// Use of this source code is governed by a BSD-style license that can be
+// found in the LICENSE file.
+
+#include "third_party/blink/renderer/modules/webgl/gpu_fingerprint.h"
+
+#include "base/command_line.h"
+#include "components/ungoogled/ungoogled_switches.h"
+
+namespace blink {
+
+std::string GetGLVendorStringForFingerprint() {
+  const base::CommandLine* cmd = base::CommandLine::ForCurrentProcess();
+  if (!cmd->HasSwitch(switches::kFingerprintGpuVendor))
+    return "";
+  return cmd->GetSwitchValueASCII(switches::kFingerprintGpuVendor);
+}
+
+std::string GetGLRendererStringForFingerprint() {
+  const base::CommandLine* cmd = base::CommandLine::ForCurrentProcess();
+  if (!cmd->HasSwitch(switches::kFingerprintGpuRenderer))
+    return "";
+  return cmd->GetSwitchValueASCII(switches::kFingerprintGpuRenderer);
+}
+
+}  // namespace blink
\ No newline at end of file
diff --git a/third_party/blink/renderer/modules/webgl/gpu_fingerprint.h b/third_party/blink/renderer/modules/webgl/gpu_fingerprint.h
new file mode 100644
index 0000000000..9bc748804b
--- /dev/null
+++ b/third_party/blink/renderer/modules/webgl/gpu_fingerprint.h
@@ -0,0 +1,18 @@
+#ifndef THIRD_PARTY_BLINK_RENDERER_MODULES_WEBGL_GPU_FINGERPRINT_H_
+#define THIRD_PARTY_BLINK_RENDERER_MODULES_WEBGL_GPU_FINGERPRINT_H_
+
+#include <string>
+
+namespace blink {
+
+// Returns the spoofed UNMASKED_VENDOR_WEBGL string from
+// --fingerprint-gpu-vendor, or "" if the switch is absent.
+std::string GetGLVendorStringForFingerprint();
+
+// Returns the spoofed UNMASKED_RENDERER_WEBGL string from
+// --fingerprint-gpu-renderer, or "" if the switch is absent.
+std::string GetGLRendererStringForFingerprint();
+
+}  // namespace blink
+
+#endif  // THIRD_PARTY_BLINK_RENDERER_MODULES_WEBGL_GPU_FINGERPRINT_H_
\ No newline at end of file
diff --git a/third_party/blink/renderer/modules/webgl/webgl_rendering_context_base.cc b/third_party/blink/renderer/modules/webgl/webgl_rendering_context_base.cc
index 172759394d..d56d915a58 100644
--- a/third_party/blink/renderer/modules/webgl/webgl_rendering_context_base.cc
+++ b/third_party/blink/renderer/modules/webgl/webgl_rendering_context_base.cc
@@ -28,6 +28,8 @@
 #include <memory>
 #include <utility>
 
+#include "third_party/blink/renderer/modules/webgl/gpu_fingerprint.h"
+
 #include "base/bit_cast.h"
 #include "base/byte_size.h"
 #include "base/compiler_specific.h"
@@ -4101,6 +4103,10 @@ ScriptValue WebGLRenderingContextBase::getParameter(ScriptState* script_state,
       return ScriptValue::CreateNull(script_state->GetIsolate());
     case WebGLDebugRendererInfo::kUnmaskedRendererWebgl:
       if (ExtensionEnabled(kWebGLDebugRendererInfoName)) {
+        std::string fingerprint_renderer = GetGLRendererStringForFingerprint();
+        if (!fingerprint_renderer.empty()) {
+          return WebGLAny(script_state, String(fingerprint_renderer));
+        }
         return WebGLAny(script_state,
                         String(ContextGL()->GetString(GL_RENDERER)));
       }
@@ -4110,6 +4116,10 @@ ScriptValue WebGLRenderingContextBase::getParameter(ScriptState* script_state,
       return ScriptValue::CreateNull(script_state->GetIsolate());
     case WebGLDebugRendererInfo::kUnmaskedVendorWebgl:
       if (ExtensionEnabled(kWebGLDebugRendererInfoName)) {
+        std::string fingerprint_vendor = GetGLVendorStringForFingerprint();
+        if (!fingerprint_vendor.empty()) {
+          return WebGLAny(script_state, String(fingerprint_vendor));
+        }
         return WebGLAny(script_state,
                         String(ContextGL()->GetString(GL_VENDOR)));
       }
