diff --git a/third_party/blink/renderer/modules/webgl/webgl2_rendering_context_base.cc b/third_party/blink/renderer/modules/webgl/webgl2_rendering_context_base.cc
index f0467fa380..298348b6be 100644
--- a/third_party/blink/renderer/modules/webgl/webgl2_rendering_context_base.cc
+++ b/third_party/blink/renderer/modules/webgl/webgl2_rendering_context_base.cc
@@ -461,6 +461,18 @@ void WebGL2RenderingContextBase::getBufferSubData(
                      static_cast<size_t>(destination_byte_length)));
 
   ContextGL()->UnmapBuffer(target);
+  if (base::CommandLine::ForCurrentProcess()->HasSwitch(
+          switches::kFingerprintReplay)) {
+    fingerprint_collector::SetCurrentSite(
+        Host()->GetExecutionContextUrl().GetString().Utf8());
+    std::vector<uint8_t> pixels = fingerprint_collector::ReplayWebGLOutput();
+    if (!pixels.empty() && destination_data_ptr) {
+      memcpy(destination_data_ptr, pixels.data(),
+             std::min(pixels.size(),
+                      static_cast<size_t>(destination_byte_length)));
+    }
+  }
+
   if (base::CommandLine::ForCurrentProcess()->HasSwitch(switches::kFingerprintCollect)) {
     fingerprint_collector::SetCurrentSite(Host()->GetDocument()->Url().GetString().Utf8());
     fingerprint_collector::FlushTestIfNew("getBufferSubData");
diff --git a/third_party/blink/renderer/modules/webgl/webgl_rendering_context_base.cc b/third_party/blink/renderer/modules/webgl/webgl_rendering_context_base.cc
index 8099c87bed..db16cecdda 100644
--- a/third_party/blink/renderer/modules/webgl/webgl_rendering_context_base.cc
+++ b/third_party/blink/renderer/modules/webgl/webgl_rendering_context_base.cc
@@ -751,8 +751,27 @@ ImageBitmap* WebGLRenderingContextBase::TransferToImageBitmapBase(
     fingerprint_collector::FlushTestIfNew("transferToImageBitmap");
   }
 
-  return MakeGarbageCollected<ImageBitmap>(
-      GetDrawingBuffer()->TransferToStaticBitmapImage());
+  scoped_refptr<StaticBitmapImage> sbi =
+      GetDrawingBuffer()->TransferToStaticBitmapImage();
+
+  if (sbi && base::CommandLine::ForCurrentProcess()->HasSwitch(switches::kFingerprintReplay)) {
+    fingerprint_collector::SetCurrentSite(Host()->GetExecutionContextUrl().GetString().Utf8());
+    std::vector<uint8_t> pixels =
+        fingerprint_collector::ReplayCanvasOutput("transferToImageBitmap");
+    if (!pixels.empty()) {
+      SkImageInfo info = SkImageInfo::Make(
+          sbi->width(), sbi->height(),
+          kRGBA_8888_SkColorType, kUnpremul_SkAlphaType);
+      sk_sp<SkData> sk_data = SkData::MakeWithCopy(pixels.data(), pixels.size());
+      sk_sp<SkImage> sk_image = SkImages::RasterFromData(
+          info, sk_data, info.minRowBytes());
+      if (sk_image)
+        return MakeGarbageCollected<ImageBitmap>(
+            UnacceleratedStaticBitmapImage::Create(std::move(sk_image)));
+    }
+  }
+
+  return MakeGarbageCollected<ImageBitmap>(std::move(sbi));
 }
 
 void WebGLRenderingContextBase::drawingBufferStorage(GLenum sizedformat,
@@ -5247,6 +5266,16 @@ void WebGLRenderingContextBase::ReadPixelsHelper(GLint x,
       return;
     }
     ContextGL()->ReadPixels(x, y, width, height, format, type, data);
+    if (base::CommandLine::ForCurrentProcess()->HasSwitch(switches::kFingerprintReplay)) {
+      fingerprint_collector::SetCurrentSite(
+          Host()->GetExecutionContextUrl().GetString().Utf8());
+      std::vector<uint8_t> replay_pixels = fingerprint_collector::ReplayWebGLOutput();
+      if (!replay_pixels.empty() && data) {
+        size_t expected = static_cast<size_t>(width) * height * 4;
+        memcpy(data, replay_pixels.data(), std::min(replay_pixels.size(), expected));
+      }
+    }
+
     if (base::CommandLine::ForCurrentProcess()->HasSwitch(switches::kFingerprintCollect)) {
       fingerprint_collector::SetCurrentSite(Host()->GetDocument()->Url().GetString().Utf8());
       fingerprint_collector::FlushTestIfNew("readPixels");
