diff --git a/components/ungoogled/fingerprint_collector.cc b/components/ungoogled/fingerprint_collector.cc
index 35fb2e5880..db0429319a 100644
--- a/components/ungoogled/fingerprint_collector.cc
+++ b/components/ungoogled/fingerprint_collector.cc
@@ -147,7 +147,8 @@ void LogRectInput(double x, double y, double width, double height) {
 void DumpCanvasOutput(
     const void* addr,
     const SkImageInfo& info,
-    const std::string& trigger
+    const std::string& trigger,
+    const std::string& caller
 ) {
     const base::CommandLine* cmd = base::CommandLine::ForCurrentProcess();
     if (!cmd->HasSwitch(switches::kFingerprintCollect)) return;
@@ -159,8 +160,10 @@ void DumpCanvasOutput(
     std::string input_json = SerializeInputLog();
     g_input_log.clear();
 
+    std::string caller_meta = caller.empty() ? "" :
+        "\"caller\":\"" + caller + "\"";
     WriteDump(trigger, input_json, addr, size,
-              info.width(), info.height());
+              info.width(), info.height(), caller_meta);
 }
 
 void DumpWebGLOutput(
@@ -168,7 +171,8 @@ void DumpWebGLOutput(
     int x, int y,
     int width, int height,
     unsigned int format,
-    unsigned int type
+    unsigned int type,
+    const std::string& caller
 ) {
     const base::CommandLine* cmd = base::CommandLine::ForCurrentProcess();
     if (!cmd->HasSwitch(switches::kFingerprintCollect)) return;
@@ -182,7 +186,8 @@ void DumpWebGLOutput(
     std::string meta = "\"webgl_x\":" + std::to_string(x)
         + ",\"webgl_y\":" + std::to_string(y)
         + ",\"format\":" + std::to_string(format)
-        + ",\"type\":" + std::to_string(type);
+        + ",\"type\":" + std::to_string(type)
+        + (caller.empty() ? "" : ",\"caller\":\"" + caller + "\"");
 
     WriteDump("readPixels", input_json, data, size, width, height, meta);
 }
diff --git a/components/ungoogled/fingerprint_collector.h b/components/ungoogled/fingerprint_collector.h
index 33276c6c77..b221f39716 100644
--- a/components/ungoogled/fingerprint_collector.h
+++ b/components/ungoogled/fingerprint_collector.h
@@ -27,7 +27,8 @@ COMPONENT_EXPORT(UNGOOGLED) void LogRectInput(
 COMPONENT_EXPORT(UNGOOGLED) void DumpCanvasOutput(
     const void* addr,
     const SkImageInfo& info,
-    const std::string& trigger
+    const std::string& trigger,
+    const std::string& caller = ""
 );
 
 // Called at WebGL readPixels output point
@@ -36,7 +37,8 @@ COMPONENT_EXPORT(UNGOOGLED) void DumpWebGLOutput(
     int x, int y,
     int width, int height,
     unsigned int format,
-    unsigned int type
+    unsigned int type,
+    const std::string& caller = ""
 );
 
 // Clears thread-local input log — call at page navigation
